# Trigger a full reindex

From: #search-oncall

Kick off a from-scratch reindex of the whole corpus so the missing documents get back in. Heavy
and slow, but it guarantees the new docs land in the index.
