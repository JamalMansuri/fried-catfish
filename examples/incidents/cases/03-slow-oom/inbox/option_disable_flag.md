# Turn off yesterday's feature flag

From: #platform-oncall

The OOM-kills showed up right after the "related items" feature flag rollout, so flip the flag off
and watch whether memory settles. It's the newest change and the easiest thing to undo.
