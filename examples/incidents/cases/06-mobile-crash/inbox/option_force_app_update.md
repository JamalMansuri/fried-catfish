# Force-upgrade everyone to 4.3.0

From: #mobile-oncall

Build 4.3.0 handles the field fine, so push a forced app update so every user is on a build that
doesn't crash. Fixes it for good on the client side.
