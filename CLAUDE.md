# Claude Code memory

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. This file imports `AGENTS.md` so Claude Code and
Codex share one doc — **edit `AGENTS.md`, not this file.** (Codex reads `AGENTS.md` directly.)

@AGENTS.md
